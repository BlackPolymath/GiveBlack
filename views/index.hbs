<!DOCTYPE html>
<html>


<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width" />
    <title>GiveBlack</title>
</head>

<body>

    {{!-- would like to add cover photo --}}
    <div class="navbar">
        <a href="/">GiveBlack</a>
    </div>


    <div class="homepage">
        {{#if message}}
        <div class="alert alert-danger">{{message}}</div>
        {{/if}}
        <a href="/login">Log In</a>
        <a href="/signup">Sign Up</a>
    </div>

    <main class="container">
        {{{ body }}}
    </main>
</body>

</html>